const {typeUserRoads }= require('./typeuser.road');
const {typeEventRoads} = require('./typeevent.road');
const {userRoads} = require('./user.road');
const {countryRoads} = require('./country.road');
const { typeActorRoads } = require('./typeactor.road');
const {eventRoads} = require('./event.road');


module.exports = {
    typeUserRoads,
    typeEventRoads,
    userRoads,
    countryRoads,
    typeActorRoads,
    eventRoads,
}